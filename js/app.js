// Search Input
const searchUser = document.getElementById('searchUser').addEventListener('keyup', (event) => {
  // Get Text Input
  const userText = event.target.value;
  // Validating That The Text Input Is Not An Empty Value
  if (userText !== '') {
    // Fetch Data From Github API Based On Input Criteria
  }
});